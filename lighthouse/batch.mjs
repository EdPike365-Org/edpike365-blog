console.log('********** Lighthouse batch.mjs **********')

// step 1: build a config object

// get a batch config
// if config file path specified, load it, otherwise use default config

// get cli args

// override config with cli args

// get chrome config

// get lighthouse config

// get urls to test

// run lighthouse

// save results

// if page passes, add it to the passed list

// if page fails, add it to the failed list
